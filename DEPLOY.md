# دليل نشر منصة Certify على Railway
### للمبتدئين — خطوة بخطوة بالتفصيل

---

## قبل البدء — ماذا ستفعل بالضبط؟

ستنشر **3 أشياء** على Railway:

```
┌─────────────────────────────────────────┐
│           مشروعك على Railway            │
│                                         │
│  1. قاعدة البيانات  ←  تخزّن كل شيء   │
│  2. certify-api     ←  الخادم الخلفي   │
│  3. certify-web     ←  الموقع الذي     │
│                         يراه المستخدم  │
└─────────────────────────────────────────┘
```

المدة الإجمالية: **20–30 دقيقة**

---

## الخطوة 0 — إنشاء حساب على Railway

1. افتح **https://railway.app**
2. اضغط **Login** ثم اختر **Login with GitHub**
3. وافق على الصلاحيات
4. ✅ أنت الآن داخل Railway

---

## الخطوة 1 — إنشاء مشروع جديد

1. اضغط الزر الكبير **New Project** في المنتصف
2. ستظهر قائمة — اختر **Deploy PostgreSQL**

   > هذا سينشئ قاعدة البيانات تلقائياً

3. انتظر 30 ثانية حتى يظهر مربع بنيّ اللون كتب عليه **Postgres**
4. ✅ قاعدة البيانات جاهزة

**احفظ رابط قاعدة البيانات:**
- اضغط على مربع **Postgres**
- افتح تبويب **Variables**
- ابحث عن **DATABASE_URL**
- اضغط على أيقونة النسخ بجانبها
- ✅ احفظه في ملاحظة مؤقتة (ستحتاجه لاحقاً)

---

## الخطوة 2 — نشر الخادم (certify-api)

### 2-A: إضافة الخدمة

1. داخل نفس المشروع، اضغط زر **+** أو **New**
2. اختر **GitHub Repo**
3. إذا طُلب منك الإذن لـ GitHub — وافق
4. ابحث عن `alfady-branch` واختره
5. سيظهر مربع جديد في المشروع — **لا تضغط Deploy بعد**

### 2-B: ضبط الإعدادات

1. اضغط على المربع الجديد لفتحه
2. افتح تبويب **Settings**
3. ابحث عن حقل **Root Directory** واكتب فيه:
   ```
   certify-api
   ```
4. ابحث عن حقل **Branch** وتأكد أنه:
   ```
   claude/project-selection-tq9yfe
   ```
5. في نفس الصفحة ابحث عن **Builder** — يجب أن يكتشف **Dockerfile** تلقائياً

### 2-C: إضافة متغيرات البيئة

1. افتح تبويب **Variables**
2. اضغط **New Variable** وأضف كل سطر من هذه الجدول:

| الاسم (Name) | القيمة (Value) |
|-------------|---------------|
| `DATABASE_URL` | الصق ما نسخته من الخطوة 1 |
| `APP_KEY` | افتح **https://generate-secret.vercel.app/64** وانسخ الناتج |
| `APP_URL` | اتركه **فارغاً الآن** (سنملؤه بعد قليل) |
| `CERTIFY_VERIFY_BASE_URL` | اتركه **فارغاً الآن** |
| `CORS_ALLOWED_ORIGINS` | اتركه **فارغاً الآن** |
| `NODE_ENV` | `production` |
| `ADMIN_EMAIL` | بريد دخولك كمدير المنصة (مثل `owner@yourdomain.com`) |
| `ADMIN_PASSWORD` | كلمة مرور قوية لحساب المدير (٨ أحرف على الأقل) |
| `ADMIN_NAME` | اسمك (مثل `مدير المنصة`) |

   > **مهم:** احفظ قيمة APP_KEY في مكان آمن — لا تغيّرها أبداً لاحقاً
   >
   > **حساب المدير:** يُنشأ تلقائياً عند أول إقلاع. بعد النشر، ادخل من صفحة
   > `/login` العادية بـ `ADMIN_EMAIL` و`ADMIN_PASSWORD` لتصل إلى لوحة الإدارة
   > `/admin`. لتغيير كلمة مرور المدير: عدّل `ADMIN_PASSWORD` وأعد النشر.

### 2-D: إضافة مساحة تخزين دائمة (Volume)

هذا مهم جداً — بدونه ستُحذف ملفات PDF عند كل إعادة تشغيل

1. افتح تبويب **Volumes** (أو ابحث عنه في القائمة الجانبية)
2. اضغط **Add Volume**
3. في حقل **Mount Path** اكتب:
   ```
   /app/storage
   ```
4. اضغط **Add**

### 2-E: النشر

1. ارجع لتبويب **Deployments**
2. اضغط **Deploy** أو انتظر حتى يبدأ تلقائياً
3. ⏳ انتظر 3–5 دقائق — سترى سجلات تظهر على الشاشة
4. عندما يصبح اللون **أخضر** وتقرأ `Certify API listening on 0.0.0.0:8000` ✅ تم

### 2-F: احفظ رابط الخادم

1. افتح تبويب **Settings**
2. ابحث عن **Networking → Generate Domain**
3. اضغط **Generate Domain**
4. ستظهر رابط مثل: `certify-api-production.up.railway.app`
5. **انسخ هذا الرابط واحفظه** — ستحتاجه كثيراً

### 2-G: اختبر أن الخادم يعمل

افتح في المتصفح:
```
https://[رابطك هنا]/api/health
```
يجب أن يظهر:
```json
{"status":"ok"}
```
✅ إذا ظهر هذا — الخادم يعمل

---

## الخطوة 3 — نشر الموقع (certify-web)

### 3-A: إضافة الخدمة

1. داخل نفس المشروع، اضغط **+** أو **New** مرة ثانية
2. اختر **GitHub Repo**
3. اختر نفس المستودع `alfady-branch`

### 3-B: ضبط الإعدادات

1. افتح تبويب **Settings**
2. في حقل **Root Directory** اكتب:
   ```
   certify-web
   ```
3. في حقل **Branch**:
   ```
   claude/project-selection-tq9yfe
   ```

### 3-C: إضافة متغير البيئة

1. افتح تبويب **Variables**
2. أضف متغيراً واحداً فقط:

| الاسم | القيمة |
|------|--------|
| `NEXT_PUBLIC_API_URL` | الرابط الذي حفظته في الخطوة 2-F (بدون `/` في الآخر) |

   مثال:
   ```
   https://certify-api-production.up.railway.app
   ```

### 3-D: النشر

1. اضغط **Deploy**
2. ⏳ انتظر 5–7 دقائق (Next.js يحتاج وقت أطول للبناء)
3. عندما يصبح أخضر ✅ تم

### 3-E: احفظ رابط الموقع

1. افتح تبويب **Settings → Networking → Generate Domain**
2. اضغط **Generate Domain**
3. احفظ الرابط — هذا **الرابط الذي ستشاركه مع العملاء** مثل:
   ```
   certify-web-production.up.railway.app
   ```

---

## الخطوة 4 — ربط الخدمتين ببعض (مهم)

الآن عندنا رابط الموقع — نحتاج نخبر الخادم به.

1. ارجع لخدمة **certify-api**
2. افتح تبويب **Variables**
3. حدّث هذه المتغيرات الثلاثة التي تركتها فارغة:

| الاسم | القيمة |
|------|--------|
| `APP_URL` | رابط certify-api (من الخطوة 2-F) |
| `CERTIFY_VERIFY_BASE_URL` | رابط certify-web (من الخطوة 3-E) |
| `CORS_ALLOWED_ORIGINS` | رابط certify-web (من الخطوة 3-E) |

4. بعد حفظ المتغيرات، اضغط **Redeploy** لإعادة تشغيل الخادم بالقيم الجديدة
5. انتظر حتى يصبح أخضر مجدداً ✅

---

## الخطوة 5 — التأكد من أن كل شيء يعمل

افعل هذه الاختبارات بالترتيب:

**اختبار 1 — الصفحة الرئيسية:**
- افتح رابط certify-web في المتصفح
- يجب أن تظهر صفحة منصة Certify ✅

**اختبار 2 — إنشاء حساب:**
- اضغط **سجّل** وأنشئ حسابك الخاص
- يجب أن تدخل للوحة التحكم ✅

**اختبار 3 — إصدار شهادة تجريبية:**
- من لوحة التحكم اضغط **إصدار شهادة**
- أدخل أي اسم واضغط إصدار
- يجب أن يُولَّد ملف PDF ✅

**اختبار 4 — صفحة التحقق:**
- افتح في المتصفح:
  ```
  https://[رابط certify-web]/verify/CERT-SMOK-0001
  ```
- يجب أن تظهر شهادة تجريبية ✅

---

## مشكلة شائعة وحلّها

| المشكلة | السبب المحتمل | الحل |
|---------|--------------|------|
| الخادم لا يبدأ | APP_KEY ناقص أو قصير جداً | تأكد أنه 32 حرفاً على الأقل |
| صفحة بيضاء في الموقع | NEXT_PUBLIC_API_URL خاطئ | تأكد أن الرابط صحيح بدون `/` في الآخر |
| PDF لا يُولَّد | مشكلة في Volume | تأكد أن Mount Path هو `/app/storage` |
| خطأ CORS | CORS_ALLOWED_ORIGINS خاطئ | يجب أن يطابق رابط certify-web بالضبط |

---

## بعد النشر — احتفظ بهذه المعلومات

```
APP_KEY      = [القيمة التي ولّدتها — احفظها جيداً]
API URL      = https://certify-api-...up.railway.app
WEBSITE URL  = https://certify-web-...up.railway.app
```

**لا تشارك APP_KEY مع أحد ولا تغيّره أبداً.**
