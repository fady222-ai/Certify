# نشر Certify على Railway

دليل نشر المنصة (واجهة Next.js + واجهة برمجية Node/Express + PostgreSQL).
المشروع جاهز للنشر — اتبع الخطوات بالترتيب.

> **مهم:** النشر يتم على حسابك في Railway. أنشئ حساباً على https://railway.app
> أولاً (تسجيل عبر GitHub أسهل، لأن المستودع موجود على GitHub).

---

## المعمارية

| الخدمة | المجلد | التقنية | المنفذ |
|--------|--------|---------|--------|
| `certify-api` | `/certify-api` | Node + Express + Prisma + Chromium | 8000 |
| `certify-web` | `/certify-web` | Next.js 16 (standalone) | 3000 |
| قاعدة البيانات | — | PostgreSQL (إضافة Railway) | — |

السبب في احتياجنا لإعداد إنتاج خاص:
- **Puppeteer/Chromium** لتوليد PDF عربي → موفّر عبر `Dockerfile`.
- **PostgreSQL** بدل SQLite (نظام ملفات Railway مؤقّت) → يُبدّل تلقائياً في `Dockerfile`.
- **تخزين الملفات** (PDF/الشعارات) → يحتاج **Volume** دائم وإلا تُفقد عند إعادة التشغيل.

---

## الخطوة 1 — قاعدة البيانات

1. في Railway: **New Project → Deploy PostgreSQL**.
2. انسخ متغيّر `DATABASE_URL` من تبويب **Variables** للقاعدة (ستستخدمه الواجهة البرمجية).

## الخطوة 2 — نشر الواجهة البرمجية (`certify-api`)

1. **New → GitHub Repo** واختر `fady222-ai/alfady-branch`.
2. في إعدادات الخدمة:
   - **Root Directory:** `certify-api`
   - **Builder:** Dockerfile (يُكتشف تلقائياً).
3. أضف متغيّرات البيئة (**Variables**):

   | المتغيّر | القيمة |
   |---------|--------|
   | `DATABASE_URL` | الصق رابط Postgres من الخطوة 1 |
   | `APP_KEY` | سلسلة عشوائية طويلة (سرّ توقيع البصمة/JWT) |
   | `APP_URL` | رابط هذه الخدمة العام (مثل `https://certify-api.up.railway.app`) |
   | `CERTIFY_VERIFY_BASE_URL` | رابط الواجهة الأمامية (الخطوة 3) |
   | `CORS_ALLOWED_ORIGINS` | رابط الواجهة الأمامية |
   | `RESEND_API_KEY` | *(اختياري)* لتفعيل الإيميل الحقيقي — بدونه يُسجَّل للّوج |
   | `EMAIL_FROM` | *(اختياري)* مثل `Certify <no-reply@yourdomain.com>` |

4. **Volume:** أضف Volume ثابتاً وثبّته على المسار `/app/storage`
   (لحفظ ملفات PDF والشعارات بين عمليات إعادة التشغيل).
5. انشر. عند أول إقلاع: يُنشئ الجداول، يزرع الباقات والبيانات التجريبية، ثم يشغّل الخادم.

> تحقّق من الصحة: افتح `https://<api-url>/api/health` → يجب أن يرجع `{"status":"ok"}`.

## الخطوة 3 — نشر الواجهة الأمامية (`certify-web`)

1. في نفس المشروع: **New → GitHub Repo** ونفس المستودع.
2. الإعدادات:
   - **Root Directory:** `certify-web`
   - **Builder:** Nixpacks (يكتشف Next.js)، أو Dockerfile إن أضفته لاحقاً.
   - **Build:** `npm run build` · **Start:** `npm start`
3. متغيّر البيئة (مطلوب **وقت البناء** لأنه يُدمج في الواجهة):

   | المتغيّر | القيمة |
   |---------|--------|
   | `NEXT_PUBLIC_API_URL` | رابط الواجهة البرمجية من الخطوة 2 |

4. انشر، ثم انسخ رابط الواجهة العام.

## الخطوة 4 — ربط الطرفين

ارجع لخدمة `certify-api` وحدّث (إن لم تكن ضبطتها):
- `CERTIFY_VERIFY_BASE_URL` = رابط `certify-web`
- `CORS_ALLOWED_ORIGINS` = رابط `certify-web`

أعد نشر `certify-api` ليأخذ القيم الجديدة.

---

## التحقق بعد النشر

1. افتح رابط `certify-web` → الصفحة الرئيسية.
2. أنشئ حساباً جديداً → لوحة التحكم.
3. أصدر شهادة → تأكّد من توليد الـ PDF وفتح صفحة `/verify/<code>`.
4. جرّب رمز التحقق التجريبي: `/verify/CERT-SMOK-0001`.

## بيانات الدخول التجريبية (من البذور)
- منظمة: «أكاديمية المسار للتدريب» · شهادة: `CERT-SMOK-0001`
  (الحساب التجريبي بلا كلمة مرور؛ أنشئ حسابك الخاص للاستخدام الفعلي.)

---

## ملاحظات الإنتاج

- **الإيميل:** بدون `RESEND_API_KEY` تُسجَّل الرسائل في اللوج فقط. فعّل Resend
  وأضف توثيق النطاق (SPF/DKIM) لتجنّب الـ Spam.
- **المدفوعات:** تبديل الباقات يعمل بوضع تجريبي؛ ربط Stripe الفعلي يحتاج مفاتيح
  Stripe (غير مفعّل بعد).
- **التخزين للنطاق الواسع:** عند آلاف الشهادات، انقل التخزين إلى S3/Cloudflare R2
  بدل قرص الخادم.
- **النطاق المخصّص:** أضِفه من إعدادات كل خدمة في Railway، ثم حدّث المتغيّرات أعلاه.

## بدائل النشر
نفس `Dockerfile` يعمل على Render أو Fly.io أو أي مزوّد يدعم الحاويات — غيّر فقط
طريقة ضبط المتغيّرات وإضافة Postgres + Volume.
