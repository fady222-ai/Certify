# 🚀 قائمة ما قبل الإطلاق للجمهور — Certify

مرجع تشغيلي مختصر يجمع كل ما يلزم قبل فتح المنصة للعملاء. التفاصيل خطوة بخطوة في
`DEPLOY.md`. رتّب التنفيذ من الأعلى للأسفل.

---

## 🔴 حرِج — لا تُطلِق قبل إنجازها

| # | البند | لماذا | أين |
|---|------|-------|-----|
| 1 | **مفتاح Resend + دومين بريد مخصّص** (`RESEND_API_KEY`, `EMAIL_FROM`) | بدونه لا تصل رموز OTP فلا يستطيع أحد التسجيل | متغيّرات بيئة الخادم + لوحة Resend (DNS) |
| 2 | **مساحة تخزين دائمة (Volume)** على `/app/storage` | بدونها تُحذف ملفات PDF عند كل إعادة نشر وتتكسّر روابط التحقق | Railway → Volumes (خطوة 2-D) |
| 3 | **بوّابة دفع واحدة على الأقل + سرّ webhook** | في الإنتاج الدفع عبر بوّابة غير مهيّأة يُرفَض (لا دخل) | `/admin/payment-gateways` أو متغيّرات البيئة |
| 4 | **`APP_KEY` قوي (32+ حرفاً)** | يحمي JWT وبصمة الشهادات؛ الخادم يرفض الإقلاع بمفتاح ضعيف | متغيّرات بيئة الخادم |
| 5 | **`NODE_ENV=production`** | يفعّل حُرّاس الأمان (رفض webhook بلا توقيع، ورفض الدفع عبر بوّابة غير مهيّأة) | مضبوط في Dockerfile + متغيّرات Railway |
| 6 | **متغيّرات الدومين الحقيقي** | بدونها CORS والروابط مكسورة | `APP_URL`, `CERTIFY_VERIFY_BASE_URL`, `CORS_ALLOWED_ORIGINS`, `NEXT_PUBLIC_API_URL`, `NEXT_PUBLIC_SITE_URL` |
| 7 | **حساب أدمن** (`ADMIN_EMAIL`, `ADMIN_PASSWORD` 8+ أحرف) | للوصول للوحة الإدارة وضبط البوّابات | متغيّرات بيئة الخادم |

**اختبار قبول سريع:** أنشئ حساباً تجريبياً → وصل رمز OTP → سجّل الدخول → أصدر شهادة →
افتح صفحة التحقق العامة → جرّب الترقية لباقة مدفوعة (وضع اختبار البوّابة).

---

## 🟡 مهمّ — للأمان والاستمرارية

- [ ] **نسخ احتياطي دوري لقاعدة البيانات** (Railway PostgreSQL) + تجربة استرجاع واحدة.
- [ ] **مراقبة الأخطاء:** اضبط `ERROR_WEBHOOK_URL` (Slack/Discord) لتصلك تنبيهات 5xx.
- [ ] **الصفحات القانونية:** `/terms`, `/privacy`, `/refund` موجودة — راجِع نصوصها مع
  مستشار قانوني (بوّابات الدفع غالباً تطلب روابطها للموافقة على الحساب).
- [ ] **سعر صرف Paymob** (`PAYMOB_USD_TO_EGP_RATE`) محدّث إن فعّلت Paymob.

---

## 🟢 تحسينات لاحقة (بعد الإطلاق)

- [ ] الانتقال من `prisma db push` إلى migrations (`prisma migrate deploy`) — راجع
  «الانتقال إلى migrations» في `DEPLOY.md` (الـbaseline جاهز في `prisma/migrations/0_init`).
- [ ] تخزين كائني (S3 / Cloudflare R2) لملفات PDF بدل قرص الخادم عند التوسّع.
- [ ] تحليلات الاستخدام (Plausible/GA) + موافقة الكوكيز.
- [ ] صفحة دعم/تواصل فعلية واختبارات E2E.

---

## ✅ جاهز فعلاً في المشروع

أمان (helmet/CORS/حدود معدّل/JWT/OTP/تشفير أسرار البوّابات/webhooks موقّعة/حارس دفع
إنتاجي) · لوحات كاملة (شهادات، قوالب، إصدار جماعي، فوترة، أدمن) · تحقق عام + QR ·
صفحات قانونية + SEO (robots/sitemap/manifest/OpenGraph) · CI (اختبارات الطرفين) ·
`GET /api/health` · معالج أخطاء لا يسرّب stack.
